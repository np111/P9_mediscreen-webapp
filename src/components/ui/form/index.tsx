import {Form as AntForm, FormItemProps as AntFormItemProps, FormProps as AntFormProps} from 'antd';

export type FormProps = AntFormProps;
export const Form = AntForm;

export type FormItemProps = AntFormItemProps;
export const FormItem = AntForm.Item;
